# -------------------------------------------------------
# Active profile controls which database implementation is used:
#   (default) = in-memory beans  (no extra profile needed)
#   sqlite    = SQLite file-based database
#
# To switch to SQLite, change the line below to:
#   spring.profiles.active=sqlite
# -------------------------------------------------------
spring.profiles.active=sqlite
