# Tell devtools to load these packages from the parent classloader,
# not the restart classloader. This is required for the SQLite JDBC driver
# and hibernate-community-dialects (SQLiteDialect) to be visible during hot reload.
restart.include.sqlite-jdbc=/sqlite-jdbc.*\.jar
restart.include.hibernate-community=/hibernate-community-dialects.*\.jar